container_commands:
  99_seed_data:
    command: |
      cd /home/mind-and-machine/mind-and-machine && 
      rails db:seed_sample_data RAILS_ENV=production
    leader_only: true    # only run on leader instance
    ignoreErrors: true   # ignore errors if data already exists